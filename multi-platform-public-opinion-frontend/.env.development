# 开发环境配置（自动覆盖 .env 中的同名变量）
NODE_ENV=development        # 显式声明开发环境（可选，默认已生效）
VUE_APP_BASE_API=http://localhost:8080  # 指向本地开发服务器
VUE_APP_USE_MOCK=true       # 启用 Mock（开发阶段常用）
VUE_APP_DEBUG=true          # 启用浏览器调试工具（如 Vue Devtools）